services:
  api:
    command: >
      uvicorn --factory app.main:create_app
      --host 0.0.0.0
      --port 8080
      --timeout-keep-alive 60
    environment:
      MACS_MAX_BODY_BYTES: "${MACS_MAX_BODY_BYTES}"
